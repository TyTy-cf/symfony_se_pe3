{% extends 'base.html.twig' %}


{% block title %}

{% endblock %}

{% block body %}

    <div class="container">
        <p class="text-center">
            {{ account.totalGameTime|time_to_string }}
        </p>
        <div class="row">
            {% for library in account.libraries %}
                <div class="col-lg-3">
                    {{ library.gameTime|time_to_string  }}
                </div>
            {% endfor %}
        </div>
    </div>

{% endblock %}

